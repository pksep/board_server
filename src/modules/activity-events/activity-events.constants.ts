export enum ActivityEntityType {
  Task = 'task',
  Board = 'board',
  Column = 'column',
  Project = 'project'
}

export enum ActivityActionType {
  Created = 'created',
  Updated = 'updated',
  Moved = 'moved',
  Deleted = 'deleted'
}
